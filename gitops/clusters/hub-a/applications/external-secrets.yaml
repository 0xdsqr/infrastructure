# Generated by `nix run .#gitops-generate`.
# External Secrets Operator is manual during first Vault adoption so CRD/controller rollout is deliberate.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/instance: external-secrets
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: external-secrets
    app.kubernetes.io/part-of: cluster-secrets
    app.kubernetes.io/version: 2.8.0
    homelab.dev/owner: platform
    homelab.dev/tier: platform-addon
spec:
  project: platform-external-secrets
  sources:
    - repoURL: https://charts.external-secrets.io
      chart: external-secrets
      targetRevision: 2.8.0
      helm:
        releaseName: external-secrets
        valueFiles:
          - $values/gitops/values/external-secrets/common.yaml
          - $values/gitops/values/external-secrets/hub-a.yaml
    - repoURL: https://github.com/0xdsqr/infrastructure.git
      targetRevision: refs/heads/master
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
      - ServerSideApply=true
