# Generated by `nix run .#gitops-generate`.
# Prefer cluster-specific overrides as Kustomize patches in this directory's kustomization.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-state-metrics
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/instance: kube-state-metrics
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: kube-state-metrics
    app.kubernetes.io/part-of: observability
    app.kubernetes.io/version: 7.2.2
    homelab.dev/owner: platform
    homelab.dev/tier: platform-addon
spec:
  project: platform-kube-state-metrics
  sources:
    - repoURL: https://prometheus-community.github.io/helm-charts
      chart: kube-state-metrics
      targetRevision: 7.2.2
      helm:
        releaseName: kube-state-metrics
        valueFiles:
          - $values/gitops/values/kube-state-metrics/common.yaml
          - $values/gitops/values/kube-state-metrics/hub-a.yaml
    - repoURL: https://github.com/0xdsqr/infrastructure.git
      targetRevision: refs/heads/master
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
