# Generated by `nix run .#gitops-generate`.
# Prefer cluster-specific overrides as Kustomize patches in this directory's kustomization.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/instance: traefik
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: traefik
    app.kubernetes.io/part-of: cluster-ingress
    app.kubernetes.io/version: 39.0.7
    homelab.dev/owner: platform
    homelab.dev/tier: platform-addon
spec:
  project: platform-traefik
  sources:
    - repoURL: https://traefik.github.io/charts
      chart: traefik
      targetRevision: 39.0.7
      helm:
        releaseName: traefik
        valueFiles:
          - $values/gitops/values/traefik/common.yaml
          - $values/gitops/values/traefik/hub-a.yaml
    - repoURL: https://github.com/0xdsqr/infrastructure.git
      targetRevision: refs/heads/master
      ref: values
      path: gitops/components/traefik/overlays/hub-a
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
