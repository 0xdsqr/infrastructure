# Generated by `nix run .#gitops-generate`.
# Shared Gateway resources reconcile only after networking, MetalLB, and origin TLS are ready.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  labels:
    app.kubernetes.io/instance: gateway
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: cluster-ingress
    platform.dsqr.dev/owner: platform
    platform.dsqr.dev/tier: platform-addon
spec:
  project: platform-gateway
  source:
    repoURL: https://github.com/0xdsqr/infrastructure.git
    targetRevision: refs/heads/master
    path: gitops/components/gateway/overlays/indigo
  destination:
    server: https://kubernetes.default.svc
    namespace: gateway-system
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
      - ServerSideApply=true
