# Generated by `nix run .#gitops-generate`.
# Prefer cluster-specific overrides as Kustomize patches in this directory's kustomization.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app.kubernetes.io/instance: metallb
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: metallb
    app.kubernetes.io/part-of: cluster-networking
    app.kubernetes.io/version: 0.16.1
    platform.dsqr.dev/owner: platform
    platform.dsqr.dev/tier: platform-addon
spec:
  project: platform-metallb
  sources:
    - repoURL: https://metallb.github.io/metallb
      chart: metallb
      targetRevision: 0.16.1
      helm:
        releaseName: metallb
        valueFiles:
          - $values/gitops/components/metallb/base/values-common.yaml
          - $values/gitops/components/metallb/overlays/indigo/values-overrides.yaml
    - repoURL: https://github.com/0xdsqr/infrastructure.git
      targetRevision: refs/heads/master
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: metallb-system
  syncPolicy:
    automated:
      enabled: false
      prune: false
      selfHeal: false
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
