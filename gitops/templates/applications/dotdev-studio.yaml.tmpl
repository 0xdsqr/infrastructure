# Generated by `nix run .#gitops-generate`.
# Cluster-specific values are loaded through the ref-only values source below.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dotdev-studio
  namespace: ${ARGOCD_NAMESPACE}
  annotations:
    argocd.argoproj.io/sync-wave: "4"
    platform.dsqr.dev/hostname: studio.dsqr.dev
  labels:
    app.kubernetes.io/instance: dotdev-studio
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/name: dotdev-studio
    app.kubernetes.io/part-of: dsqr-dotdev
    platform.dsqr.dev/owner: dsqr
    platform.dsqr.dev/tier: application
spec:
  project: dsqr
  sources:
    - repoURL: https://github.com/0xdsqr/dsqr-dotdev.git
      targetRevision: ${VALUES_TARGET_REVISION}
      path: helm/dotdev-studio
      helm:
        releaseName: dotdev-studio
        valueFiles:
          - values-prod.yaml
          - ${VALUES_REF}/${VALUES_OVERLAY_FILE}
    - repoURL: ${VALUES_REPO_URL}
      targetRevision: ${VALUES_TARGET_REVISION}
      ref: values
  destination:
    server: ${DESTINATION_SERVER}
    namespace: dsqr
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 3
      refresh: true
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 2m
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - FailOnSharedResource=true
      - PruneLast=true
