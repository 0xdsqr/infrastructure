apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  - app-default-deny.networkpolicies.yaml
  - internal-ca.configmaps.yaml
  - platform-admins.clusterrolebinding.yaml

labels:
  - pairs:
      platform.dsqr.dev/cluster: hub-a
      platform.dsqr.dev/environment: lab
