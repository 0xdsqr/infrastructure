# hub-a Helm value overrides for Cilium.
# Use this for cluster-specific API server, networking, rollout, node placement, or pod metadata settings.
cluster:
  name: hub-a
  id: 1

k8sServiceHost: 10.10.30.104
k8sServicePort: 6443
