# hub-a Helm value overrides for MetalLB.
# Use this for cluster-specific controller/speaker settings, resource sizing, node placement, or pod metadata.
# Leave empty when hub-a uses the base values unchanged.
{}
