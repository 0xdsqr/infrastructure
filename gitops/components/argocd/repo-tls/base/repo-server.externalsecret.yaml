apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-repo-server-serving-tls
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  refreshPolicy: Periodic
  # Reissue every 10 days, ahead of the overlay's 30-day certificate expiry.
  refreshInterval: 240h
  target:
    # Deliberately not argocd-repo-server-tls: the current Helm chart projects
    # that Secret's private key into clients too. Connect this serving Secret
    # only to repo-server in the subsequent, separately validated rollout.
    name: argocd-repo-server-serving-tls
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: kubernetes.io/tls
      metadata:
        labels:
          app.kubernetes.io/name: argocd-repo-server-serving-tls
          app.kubernetes.io/managed-by: external-secrets
          app.kubernetes.io/part-of: argocd
          platform.dsqr.dev/owner: platform
          platform.dsqr.dev/tier: platform-secret
      data:
        tls.crt: |
          {{ .certificate }}
          {{ .issuing_ca }}
        tls.key: |
          {{ .private_key }}
        ca.crt: |
          {{ .issuing_ca }}
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: VaultDynamicSecret
          name: argocd-repo-server-serving-tls
