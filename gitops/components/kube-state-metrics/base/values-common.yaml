# Shared default Helm values for the kube-state-metrics chart.
# Keep values here when every cluster should expose metrics the same way.
# Put cluster-specific ServiceMonitor or scrape settings in gitops/components/kube-state-metrics/overlays/<cluster>/values-overrides.yaml.
prometheus:
  monitor:
    enabled: false
