# Shared defaults for the kubelet serving-certificate CSR approver.
# Cluster identity and address allowlists belong in the cluster overlay.
leaderElection: true
replicas: 2
priorityClassName: system-cluster-critical

metrics:
  enable: true
  serviceMonitor:
    enabled: false
