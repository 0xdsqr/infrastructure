apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: cilium-metallb
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: cilium-metallb
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: cluster-ingress
    platform.dsqr.dev/owner: platform
    platform.dsqr.dev/tier: platform-addon
spec:
  controllerName: io.cilium/gateway-controller
  description: Cilium Gateway API backed by the explicit MetalLB service class
  parametersRef:
    group: cilium.io
    kind: CiliumGatewayClassConfig
    name: cilium-metallb
    namespace: gateway-system
